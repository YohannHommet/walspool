run:
  timeout: 5m
  go: '1.22'

linters:
  disable-all: true
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - typecheck

issues:
  exclude-dirs:
    - tmp
  max-issues-per-linter: 0
  max-same-issues: 0
